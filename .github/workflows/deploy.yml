name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  schedule:
    # Re-fetch budget data monthly (in case budget is updated)
    - cron: '0 6 1 * *'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Fetch budget data from obudget.org API
        run: |
          bash fetch-budget.sh
          echo "--- Files created ---"
          ls -la budget-data.json 2>/dev/null || echo "budget-data.json NOT created"

      - name: Prepare site
        run: |
          mkdir -p _site
          cp index.html _site/
          [ -f budget-data.json ] && cp budget-data.json _site/ || true

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
